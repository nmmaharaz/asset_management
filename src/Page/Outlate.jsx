import React from 'react';
import { Outlet } from 'react-router-dom';

const Outlate = () => {
    return (
        <div>
            <Outlet></Outlet>
        </div>
    );
};

export default Outlate;